# 実機での確認手順（手動）

自動テストで確かめられるのは、URL の判定と content script の組み立てまでです。
**Chrome に読み込んだ状態での動き**は自動化できないので、ここに手順を書いておきます。

ストアへ更新を出す前に、この表を1枚コピーして埋めてください。
**確認していない項目を「問題なし」として扱わないこと。** 未実施は未実施と書きます。

> **⚠️ リポジトリのフォルダをそのまま読み込んで終わりにしないこと。**
> 提出する予定の ZIP を新しい一時フォルダへ展開し、**その展開したフォルダ**を
> 「パッケージ化されていない拡張機能を読み込む」で読み込んでください。
> リポジトリのフォルダでは動くのに配布物だけ壊れている、という形の事故は、
> これでしか捕まりません。開発中の下見としてフォルダを読み込むのは構いませんが、
> 提出前の確認は必ず ZIP の展開物で行います。

## 確認した対象（提出するZIPそのもの）

| 項目 | 記入 |
|---|---|
| 対象コミット | |
| 対象バージョン | |
| ZIP のパス | |
| ZIP の SHA-256 | |
| ZIP に入っていたファイル数 | |
| 展開先のフォルダ | |

## 確認した環境

| 項目 | 記入 |
|---|---|
| 日付 | |
| OS とその版 | |
| Chrome の版（`chrome://version`） | |
| 読み込み方 | **提出ZIPの展開物** / リポジトリのフォルダ（開発中の下見） / ウェブストア版 |
| 「シークレット モードでの実行を許可」 | オン / オフ |

## 確認する項目

| # | 何を | 期待する結果 | 結果 |
|---|---|---|---|
| 1 | `chrome://extensions` でフォルダを読み込む | エラーも警告も出ない | |
| 2 | x.com / www.x.com / twitter.com / www.twitter.com でポスト本文を右クリック | 「このポストをシークレットウィンドウで開く」が出る | |
| 3 | `example.com` など対象外のサイトで右クリック | この拡張の項目が**出ない** | |
| 4 | ポスト本文を右クリックして項目を選ぶ | そのポストがシークレットウィンドウで開く | |
| 5 | ポスト本文を Shift+Alt クリック | 同じ結果になる。元のタイムラインは遷移しない | |
| 6 | プロフィールへのリンクの上で Shift+Alt クリック | 拡張は何もせず、通常どおりプロフィールへ移動する | |
| 7 | 外部記事・ハッシュタグへのリンクの上で Shift+Alt クリック | 同上（本来の遷移を奪わない） | |
| 7b | ポストのタイムスタンプ（パーマリンク）の上で Shift+Alt クリック | **そのポストが開く。** ポストのURLであるリンクは対象で、#6・#7 とは扱いが違う | |
| 7c | ポストの画像の上で Shift+Alt クリック | ポスト**本体**が開く（`/photo/1` ではない） | |
| 8 | サイドバーや余白で右クリックして項目を選ぶ | ウィンドウは開かず、画面下に短い案内が出る | |
| 8b | サイドバーや余白で Shift+Alt クリック | **何も起きない。案内の帯も出ない**（#8 の右クリック経路とはここが違う） | |
| 9 | 通常のポスト | 正しいポストが開く | |
| 10 | 引用つきのポスト（外側の本文を起点に） | **外側**のポストが開く | |
| 11 | 引用つきのポスト（引用の箱の中を起点に） | **外側**のポストが開く。引用の箱は `<a>` ではなく `div[role="link"]` なので、祖先をたどると外側の `<article>` に当たる（`role="link"` で分岐しないのは意図的。リンクプレビューのカードも同じ箱を使うため） | |
| 11b | 同じポストで、引用元の**画像**を起点に | 引用元のポストが開く（画像は `<a href=".../status/.../photo/1">` なので、そこで採られる） | |
| 12 | リポスト | 元のポストが開く | |
| 13 | 画像つきのポスト（画像を起点に） | ポスト本体が開く（`/photo/1` ではない） | |
| 14 | 動画つきのポスト | 同上 | |
| 15 | プロフィールページのポスト一覧 | 正しいポストが開く | |
| 16 | ポストの個別ページ（`/…/status/…`）の中のポスト | 正しいポストが開く | |
| 17 | 拡張を入れる**前から**開いていたタブで右クリック | 「準備ができました。もう一度お試しください」が出て、2回目は開く | |
| 18 | `chrome://extensions` で拡張を再読み込みしたあと、既存タブで右クリック | 1回の操作でウィンドウが1枚だけ開く（2枚開かない） | |
| 19 | 同上のとき DevTools の Console | listener の二重登録に由来する異常が出ていない | |
| 20 | 「シークレット モードでの実行を許可」がオフ | シークレットウィンドウが開く | |
| 21 | 同オン | 挙動が変わらない（毎回あたらしいウィンドウが開く） | |
| 22 | Chrome の言語を英語にして再起動 | メニューと通知が英語で出る | |
| 23 | 同じく日本語 | 日本語で出る | |
| 24 | DevTools の Network（拡張の service worker） | 拡張自身が出す外部通信が無い | |
| 25 | 一連の操作のあと Console | エラーが出ていない | |
| 26 | ポストを右クリックし、**メニューを押さずに1分以上待ってから**もう一度右クリックしてメニューを押す | 前のポストが開かず、正しく今のポストが開く（古い値が使われない） | |
| W1 | **Windows のみ**: ポストを Shift+Alt クリック | **Left Alt+Shift が OS 側の切替に割り当てられている構成**: ポストが開く。入力言語は OS の設定どおり切り替わり得る。**割り当てられていない構成**: ポストだけが開く。どちらでもポストが開くこと自体は変わらない | |
| W2 | **Windows のみ**: Left Alt+Shift に切替が割り当てられていない状態（入力言語が1つ、または別のホットキー）で Shift+Alt クリック | 入力言語は切り替わらず、ポストだけが開く | |
| W3 | **Windows のみ**: US-International などの配列で、**AltGr を押しながら**ポストをクリック | **何も起きない**（AltGr が Ctrl+Alt として届く場合と、`getModifierState('AltGraph')` でしか分からない場合の両方を対象外にしてある）。**Windows 実機では未確認** | |
| W4 | **Windows のみ**: 通知の帯を出す（ポストの外で右クリック→メニュー） | 文字が読める字体で表示され、崩れや豆腐が出ない | |
| W5 | **Windows のみ**: 英語ロケールの Chrome でメニューと通知を見る | 英語で表示される（メニュー: "Open this post in an Incognito window (Shift+Alt click)"） | |
| 27 | x.com の DevTools コンソールで、ページ側から合成イベントを投げる<br>`document.body.dispatchEvent(new MouseEvent('click',{bubbles:true,shiftKey:true,altKey:true}))` | **シークレットウィンドウが開かない**（`isTrusted` が false のイベントは受け取らない） | |
| 28 | 同じく合成の `contextmenu` を投げたあと、正しいポストを右クリックしてメニューを押す | 合成イベントに邪魔されず、正しいポストが開く | |
| 29 | ポストの**タイムスタンプ（リンク）を右クリック**してメニューを押し、そのあと**ポストの外**を右クリックしてメニューを押す | 1回目は正しいポストが開く。2回目は**前のポストが開かず**、「見つかりません」の帯が出る（直接開いた回の値が残っていない） | |

## 過去の記録

| 日付 | 環境 | 範囲 | 結果 |
|---|---|---|---|
| 2026-08-03 | macOS / Chrome 150 / v1.5.0〜1.6.0 / 未パッケージ読み込み | #2 #3 #4 #5 #20 | すべて期待どおり |
| 2026-08-04 | 同上（ログアウト状態の新規プロファイル） | 開いた先の表示 | ポスト6件とも本文・引用・返信が表示。ログイン画面への転送は0件。年齢制限つきメディアだけは表示されない |
| 2026-08-05 | macOS / 未パッケージ読み込み / URL判定を `post-url.js` へ移したあとの版 | #1 #4 #5 #10 #11 #18 | **6項目すべて期待どおり。** #1 はエラーも警告も無し（service worker が `importScripts('post-url.js')` を読めている）。#18 はシークレットウィンドウが1枚だけ開き、listener の二重登録は起きていない。#11 は外側のポストが開いた |
| 2026-08-05 | macOS / **提出用 `dist/postcloak-1.6.1.zip` を展開して読み込み** / main `026f5d1`・v1.6.1（**その後の未コミット差分を当てる前の記録**） | #1 #4 #5 #7b #7c #8b #18 | **7項目すべて期待どおり。** #1 エラーも警告も無し（初期化の印を立てる順序を変えたあと）。#4 右クリック経路に URL の再検証を足しても正しいポストが開く。#7b パーマリンクの上の Shift+Alt は**そのポストを開く**。#7c 画像の上では**ポスト本体**が開き、URLに `/photo/1` は残らない。#8b ポスト外の Shift+Alt では**帯も出ない**（#8 の右クリック経路との違いを実測）。#18 再読み込み後もウィンドウは1枚だけ |

> #11 の期待する結果は、2026-08-05 の実測を受けて書き直しました。**それ以前は「引用元のポストが開く」と書いていましたが、これは実装が約束していない動きでした。** 引用の箱で分岐しないのは意図的な設計です。

## まだ確かめていないこと

上の記録はいずれも **base commit `026f5d1` に対するもの**です。版の番号は同じ v1.6.1 のままなので、**版だけでは同じ中身かどうか判別できません**。以下は未コミット差分を当てたあとの状態について、まだ実機で確かめていない項目です。

- 60秒タイマーの handle の扱い（古いタイマーが新しい handle を消さないこと）
- リンクを直接開いた回に、控えていた値が消えること（#29）
- `isTrusted` による合成イベントの除外（#27・#28）
- AltGraph／AltGr の除外（W3）
- Windows 実機での W1〜W5
- コミット後の CI
- コミット名を含む提出ZIP（`postcloak-<version>-<commit>.zip`）の展開物での通し確認

いずれも自動テストでは通っていますが、**ブラウザ実機での確認とは別物**です。読み込んだ拡張の版を確かめるときは、版の番号ではなく**対象コミットとZIPの SHA-256** で照合してください。
